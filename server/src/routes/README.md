# routes

Definicion de rutas y mapeo de endpoints hacia sus controladores.

`authRoutes`: ademas de registro/login y perfil, expone `POST /forgot-password` y `POST /reset-password` (detalle en `docs/design.md`).
