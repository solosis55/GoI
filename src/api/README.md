# api

Clientes HTTP, configuracion de peticiones y modulos para consumir endpoints.

`authApi.ts`: `register`, `login`, `getProfile`, `updateProfile`, usuarios/seguimiento, y recuperacion de contraseña (`requestPasswordReset`, `resetPasswordWithToken`).
